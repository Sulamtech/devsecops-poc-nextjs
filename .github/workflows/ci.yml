name: ci

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions: {}

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  integrity:
    name: integrity
    permissions:
      contents: read
    uses: Sulamtech/devsecops-poc-workflows/.github/workflows/reusable-caller-integrity.yml@f065e771d20dd4c7966e36a8c8db685cf349a8b3

  governance:
    name: governance
    if: github.event_name == 'pull_request'
    permissions:
      contents: read
      issues: read
      pull-requests: read
    uses: Sulamtech/devsecops-poc-workflows/.github/workflows/reusable-pr-governance.yml@f065e771d20dd4c7966e36a8c8db685cf349a8b3

  tests:
    name: tests
    needs: integrity
    permissions:
      contents: read
    uses: Sulamtech/devsecops-poc-workflows/.github/workflows/reusable-node-ci.yml@f065e771d20dd4c7966e36a8c8db685cf349a8b3
    with:
      node-version: "22"

  security:
    name: security
    needs: integrity
    permissions:
      contents: read
      pull-requests: read
    uses: Sulamtech/devsecops-poc-workflows/.github/workflows/reusable-security.yml@f065e771d20dd4c7966e36a8c8db685cf349a8b3
    with:
      image-name: devsecops-poc-nextjs

  feedback:
    name: feedback
    if: ${{ always() && github.event_name == 'pull_request' }}
    needs: [integrity, tests, security]
    permissions:
      contents: read
      pull-requests: write
    uses: Sulamtech/devsecops-poc-workflows/.github/workflows/reusable-pr-feedback.yml@f065e771d20dd4c7966e36a8c8db685cf349a8b3
    with:
      integrity-result: ${{ needs.integrity.result }}
      tests-result: ${{ needs.tests.result }}
      security-result: ${{ needs.security.result }}
