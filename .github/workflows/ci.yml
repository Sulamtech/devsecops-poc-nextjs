name: ci

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions: {}

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  integrity:
    name: integrity
    permissions:
      contents: read
    uses: Sulamtech/devsecops-poc-workflows/.github/workflows/reusable-caller-integrity.yml@caccbea651ddd4d998295b7b56007ac46cf7fde7

  governance:
    name: governance
    needs: integrity
    if: github.event_name == 'pull_request'
    permissions:
      contents: read
      issues: read
      pull-requests: read
    uses: Sulamtech/devsecops-poc-workflows/.github/workflows/reusable-pr-governance.yml@caccbea651ddd4d998295b7b56007ac46cf7fde7

  tests:
    name: tests
    needs: integrity
    permissions:
      contents: read
    uses: Sulamtech/devsecops-poc-workflows/.github/workflows/reusable-node-ci.yml@caccbea651ddd4d998295b7b56007ac46cf7fde7
    with:
      node-version: "22"

  security:
    name: security
    needs: integrity
    permissions:
      contents: read
      pull-requests: read
    uses: Sulamtech/devsecops-poc-workflows/.github/workflows/reusable-security.yml@caccbea651ddd4d998295b7b56007ac46cf7fde7
    with:
      image-name: devsecops-poc-nextjs

  feedback:
    name: feedback
    if: ${{ always() && github.event_name == 'pull_request' }}
    needs: [integrity, tests, security]
    permissions:
      contents: read
      pull-requests: write
    uses: Sulamtech/devsecops-poc-workflows/.github/workflows/reusable-pr-feedback.yml@caccbea651ddd4d998295b7b56007ac46cf7fde7
    with:
      integrity-result: ${{ needs.integrity.result }}
      tests-result: ${{ needs.tests.result }}
      security-result: ${{ needs.security.result }}
